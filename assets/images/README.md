This static demo uses remote wildlife photography URLs in the HTML so the pages remain lightweight.

Place production-licensed local imagery in this folder before launch and replace the remote URLs when desired.
